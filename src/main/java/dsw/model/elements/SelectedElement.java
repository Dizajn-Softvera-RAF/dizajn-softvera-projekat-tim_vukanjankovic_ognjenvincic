package dsw.model.elements;

import java.awt.*;

public class SelectedElement extends Interclass {
    public SelectedElement(Point position, Dimension size, float stroke, Paint paint, Paint borderPaint, Paint textPaint) {
        super(position, size, stroke, paint, borderPaint, textPaint);
    }
}
